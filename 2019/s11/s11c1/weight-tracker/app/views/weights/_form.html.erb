<%= form_with(model: weight, local: true) do |form| %>
  <% if weight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(weight.errors.count, "error") %> prohibited this weight from being saved:</h2>

      <ul>
        <% weight.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value %>
  </div>

  <div class="field">
    <%= form.label :sample_date %>
    <%= form.date_select :sample_date %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.select(:user_id, @users.collect {|a| [a.name, a.id]}) -%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
